stage('Deploy') {
    steps {
        // Copy JAR from Jenkins workspace to VM
        bat "scp target\\myapp-1.0-SNAPSHOT.jar ubuntu@${DEPLOY_HOST}:/opt/myapp/myapp.jar"

        // Restart service on VM
        bat "ssh ubuntu@${DEPLOY_HOST} \"sudo systemctl restart myapp\""
    }
}
